## 1. Helper de filtragem

- [x] 1.1 Criar `filterMeuMesTransactions` em `lib/transactions/filter.ts` (busca em descrição/tags + filtro por tipo)

## 2. UI de pesquisa e filtro

- [x] 2.1 Adicionar estado local de pesquisa e filtro de tipo por seção em `MeuMesTransactionLists`
- [x] 2.2 Renderizar `Input.Search` e `Select` de tipo acima de cada lista (Pendentes e Liquidados)
- [x] 2.3 Aplicar filtragem antes do agrupamento por tag e ajustar empty states

## 3. Testes

- [x] 3.1 Testes unitários do helper de filtragem
- [x] 3.2 Testes de integração em `meu-mes-transaction-lists.test.tsx` (busca, filtro por tipo, empty state)
